This folder contains the local image assets referenced by project3.

Currently referenced image paths:
- restaurant.jpg (schema image)
- External food/gallery image URLs used in <img> elements

Add downloaded images here and update index.html to use local files if needed.